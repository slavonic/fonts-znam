# travis CI for testing font package fonts-znam
os: linux

addons:
  apt:
    packages:
      - texlive-binaries
      - texlive-latex-extra
      - texlive-luatex
      - ghostscript
      - imagemagick
      - fontforge
      - fonttools

script:
    - cd Mezenets; make test
